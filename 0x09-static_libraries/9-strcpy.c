#include "main.h"
/**
 * _strcpy - copies a string from a source to a destination.
 * @src: the source.
 * @dest: the destination.
 * 
 * Return: it returns a a pointer to the dest.
 */
char *_strcpy(char *dest, char *src)
{
}
