#include "main.h"
/**
 * _putchar - puts a character on the screen using the write()
 * function.
 * @c: the character to put (in integer).
 *
 * Return: it returns (0), success.
 */
int _putchar(char c)
{
}
