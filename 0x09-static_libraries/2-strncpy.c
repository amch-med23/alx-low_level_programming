#include "main.h"
/**
 * _strncpy - it copies n amount of characters from a given
 * string to a destination.
 * @src: the given string source.
 * @dest: the destination to copy to.
 * @n: the number of chars to copy.
 *
 * Return: it returns a pointer to the dest.
 */
char *_strncpy(char *dest, char *src, int n)
{
}
