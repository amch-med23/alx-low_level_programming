#include "main.h"
/**
 * _strcmp - it compares two given strings.
 * @s1: the first string.
 * @s2: the second string.
 *
 * Return: it return an integer determining if the 
 * two given strings are differant or not.
 */
int _strcmp(char *s1, char *s2)
{
}
