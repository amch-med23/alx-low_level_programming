#include "main.h"
/**
 * _strcat - it cuts a string from a place in memory
 * to another place.
 * @src: the source.
 * @dest: the destination.
 *
 * Return: a pointer to the cutted character.
 */
char *_strncat(char *dest, char *src, int n)
{
}
