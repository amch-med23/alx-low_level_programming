#include "main.h"
/**
 * _strpbrk - does something.
 * @s: a ppointer to a string.
 * @accept: the second parameter.
 *
 * Return: it returns an integer.
 */
char *_strpbrk(char *s, char *accept)
{
}
