#include "main.h"
/**
 * _strchr - searches for a character in a string.
 * @s: the string.
 * @c: the character.
 *
 * Return: it returns the character if found.
 */
char *_strchr(char *s, char c)
{
}
