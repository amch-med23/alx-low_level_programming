#include "main.h"
/**
 * _memcpy - copies a given amount of  memory content from a 
 * source to a given destination.
 * @src: the source.
 * @dest: the destination.
 * @n: the amount to copy.
 *
 * Return: a ppointer to the copied string.
 */
