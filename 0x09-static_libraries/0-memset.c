#include "main.h"
/**
 * _memset - the function.
 * @s: the first parameter.
 * @b: the second parameter.
 * @n: the third parameter.
 * Return: it's gonna return a pointer to the memory.
 */
char *_memset(char *s, char b, unsigned int n)
{
}
