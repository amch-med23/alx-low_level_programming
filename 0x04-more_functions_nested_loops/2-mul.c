#include "main.h"
/**
 * mul - prints the multiplication of two passed numbers.
 * @a: the first number.
 * @b: the second number
 *
 * Return: returns the result as an integer.
 */

int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
