#include "main.h"
/**
 * _islower - checks is a haracter is lowercase or not.
 * Return: it's (0) for lowercase, and (1) for uppercase.
 */

int _islower(int c);
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else if (c <= 97 && < >= 122)
	{
		return (0);
	}
}
