#include "main.h"
/**
 * add - adds the two integers.
 * @i: the first operand.
 * @j: the second operand.
 *
 * Return: Returns the value of the addition.
 */

int add(int i, int j)
{
	int result;

	result = i + j;
	return (result);
}
